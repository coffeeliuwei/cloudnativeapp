<?xml version="1.0" encoding="UTF-8"?>
<configuration  scan="false" scanPeriod="10 seconds">
    <contextName>logback</contextName>
    <property name="LOG_PATTERN" value=" 用户订单中心  %d{yyyy-MM-dd HH:mm:ss.SSS}   %msg      %-5level %logger{50} %n"/>
    <shutdownHook class="ch.qos.logback.core.hook.DelayingShutdownHook"/>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter>
        <encoder>
            <Pattern>${LOG_PATTERN}</Pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="aliyun_dev" class="com.aliyun.openservices.log.logback.LoghubAppender">
        <!--必选项-->
        <!-- 账户及网络配置 -->
        <endpoint>cn-beijing.log.aliyuncs.com</endpoint>
        <accessKeyId>${ALIYUN_ACCESS_KEY_ID}</accessKeyId>
        <accessKey>${ALIYUN_ACCESS_KEY_SECRET}</accessKey>
        <!-- sls 项目配置 -->
        <projectName>userorder</projectName>
        <logstore>logs</logstore>
        <!--必选项 (end)-->
        <!-- 可选项 -->
        <topic>订单</topic>
        <!--<source>your source</source>-->
        <!-- 可选项 -->
        <packageTimeoutInMS>30000</packageTimeoutInMS>
        <logsCountPerPackage>4096</logsCountPerPackage>
        <logsBytesPerPackage>3145728</logsBytesPerPackage>
        <memPoolSizeInByte>104857600</memPoolSizeInByte>
        <retryTimes>3</retryTimes>
        <maxIOThreadSizeInPool>8</maxIOThreadSizeInPool>
        <!-- 可选项 通过配置 encoder 的 pattern 自定义 log 的格式 -->
        <encoder>
            <Pattern>${LOG_PATTERN}</Pattern>
        </encoder>
        <!-- 可选项 设置 time 字段呈现的格式 -->
        <timeFormat>yyyy-MM-dd'T'HH:mmZ</timeFormat>
        <!-- 可选项 设置 time 字段呈现的时区 -->
        <timeZone>UTC</timeZone>
    </appender>

    <!--开发环境-->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="aliyun_dev" />
        </root>
    </springProfile>
</configuration>